services:
  mongo:
    image: mongo:7
    container_name: mongo-dev
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: nestmongo
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data: